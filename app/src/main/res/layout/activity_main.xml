<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical">

	<android.support.v7.widget.Toolbar
		android:id="@+id/toolbar"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:background="@color/colorPrimary"
		android:elevation="6dp"
		android:minHeight="?attr/actionBarSize"
		android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
		app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

	<android.support.design.widget.TabLayout
		android:id="@+id/tab_layout"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:background="@color/colorPrimary"
		android:elevation="6dp"
		android:minHeight="?attr/actionBarSize"
		android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"/>

	<android.support.v4.view.ViewPager xmlns:android="http://schemas.android.com/apk/res/android"
	                                   android:id="@+id/pager"
	                                   android:layout_width="match_parent"
	                                   android:layout_height="0dp"
	                                   android:layout_weight="1"/>

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="@dimen/bottom_bar_height"
		android:background="@color/colorPrimary"
		android:gravity="center_vertical"
		android:paddingEnd="@dimen/bottom_bar_padding"
		android:paddingStart="@dimen/bottom_bar_padding">

		<ImageView
			android:id="@+id/iv_add_min"
			android:layout_width="@dimen/bottom_bar_icons"
			android:layout_height="@dimen/bottom_bar_icons"
			android:contentDescription="@string/add_min_content_description"
			android:paddingEnd="@dimen/bottom_bar_padding"
			android:paddingStart="@dimen/bottom_bar_padding"
			android:src="@drawable/ic_action_add_min"
			android:tint="@android:color/white"/>

		<TextView
			android:id="@+id/tv_time_remaining"
			android:layout_width="0dp"
			android:layout_height="wrap_content"
			android:layout_weight="1"
			android:paddingEnd="@dimen/bottom_bar_padding"
			android:paddingStart="@dimen/bottom_bar_padding"
			android:textColor="@color/textColour"
			android:textSize="18sp"
			tools:text="Timer Remaining: 24:12:06"/>

		<ImageView
			android:id="@+id/iv_pause_timer"
			android:layout_width="@dimen/bottom_bar_icons"
			android:layout_height="@dimen/bottom_bar_icons"
			android:contentDescription="@string/pause_timer_content_description"
			android:paddingEnd="@dimen/bottom_bar_padding"
			android:paddingStart="@dimen/bottom_bar_padding"
			android:src="@drawable/ic_pause_circle_outline"
			android:tint="@android:color/white"/>

		<ImageView
			android:id="@+id/iv_stop_timer"
			android:layout_width="@dimen/bottom_bar_icons"
			android:layout_height="@dimen/bottom_bar_icons"
			android:contentDescription="@string/stop_timer_content_description"
			android:paddingEnd="@dimen/bottom_bar_padding"
			android:paddingStart="@dimen/bottom_bar_padding"
			android:src="@drawable/ic_highlight_off"
			android:tint="@android:color/white"/>

	</LinearLayout>

</LinearLayout>